import { BrazilianPhonePipe } from './brazilian-phone.pipe';

describe('BrazilianPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new BrazilianPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
